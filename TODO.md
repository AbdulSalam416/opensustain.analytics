# TODO — retiring the Streamlit dashboard

Working list for bringing the Next.js app in `web/` to full parity with
`streamlit-app-tab.py`, then putting it in production.

Six of the nine Streamlit tabs are done. The three gaps below are all
**unblocked on data** — every payload the missing work needs is already
generated by `scripts/build_analytics_payloads.py`.

Ordered by cost-to-value: B is the smallest change and retires a whole tab.

---

## B. Organisations by Sub-Category sunburst — closes tab 7

Currently an 8-item list on `/organizations`; the chart does not exist.
A two-level tree (81 sub-categories → the organisations working in each),
identical in shape to Projects by Organisation, so it reuses the existing
sunburst wholesale.

- [ ] `lib/sunburst/subcategory-tree.ts` — adapt `organizations-by-subcategory.json`
      `root.children[81]` onto `SunburstNode`, the way `org-tree.ts` does
- [ ] Wrapper component reusing `SunburstSvg` with `maxRings = 1`
      (sub-categories first, organisations after a click)
- [ ] Colour sub-categories by their parent ecosystem category so the palette
      stays consistent with the other two sunbursts
- [ ] Replace the list in `app/organizations/page.tsx`; drop
      `loadOrganizationsBySubcategory` from the server component

## A. Organisations tab — one map, four charts — closes tab 5

Currently three truncated 8-item lists. Data is all in
`organizations-overview.json`.

- [ ] **Projects per country** — choropleth. `countries[43]` already carry
      `iso_alpha` and a `map_eligible` flag.
      Cost: ECharts needs a world GeoJSON registered (~250 KB) — load it on
      demand, do not ship it in the bundle
- [ ] **Top organisations by listed projects** — bar + top-N control
      (`organizations_by_project_count[1274]`)
- [ ] **Organisations by type** — bar (`organization_type_counts[7]`)
- [ ] **Top countries by organisation count** — bar + top-N control
- [ ] **Organisations by continent** — bar (`continent_counts[6]`)
- [ ] Fetch the payload client-side once these charts consume it (527 KB)

## C. Projects Attributes — eight distribution charts — closes tab 8

Currently three lists cut at eight rows, which hides the long tail that is
the point of the tab. `project-attributes.json` is 5 KB, so it can stay
server-loaded.

- [ ] Commit activity — active vs dormant (`commit_activity[2]`)
- [ ] Six categorical distributions, each a bar with a top-N control:
      licence, language, code of conduct, contributing guide, ecosystems,
      platform (`fields.*`)

## D. Discord link in the header

- [ ] Add `https://discord.gg/JDUatGKxve` to `site-header.tsx`, styled as
      small uppercase tracked bold beside the nav, matching climatetriage.com
- [ ] Include it in the mobile disclosure, not just the desktop bar

---

## Country filters — needs a decision before starting

**Projects have no country.** There is no country column in
`data/projects.csv` and no country field in `project-rankings.json` or
`projects-over-time.json`. Only organisations carry `location_country`.

- [ ] Organisation views — a country filter works today from existing data
- [ ] **Decide:** project views need a country attached per project in
      `scripts/build_analytics_payloads.py`. The join already exists there
      (organisations → projects on a normalised git URL), but that file is
      regenerated by the `update_data` workflow, so changing it is a call
      to make deliberately
- [ ] Use `organizations-overview.countries` (43, ISO-coded), **not**
      `filters.countries` — the latter is built from raw `location_country`
      with no cleaning and contains "Berlin", a city

---

## Production blockers

Independent of feature parity. None of these are about the charts.

- [ ] **There is no deploy path for the Next.js app.** `Dockerfile` is
      `python:3.13-slim` running `streamlit run`, and `deploy-package.yaml`
      ships only that image. `.gitignore` already lists `web/out/`, so a
      static export looks like the original intent — `next.config.ts` has no
      `output` mode set
- [ ] **No CI touches the web app.** Four workflows, none run npm.
      Add a typecheck + build gate; it would have caught the type error the
      old sunburst shipped with
- [ ] **No error boundaries.** `app/` has no `error.tsx` or
      `global-error.tsx`, so any render-time throw replaces the page with
      Next's exception screen
- [ ] **No tests.** No test tooling in the repo at all
- [ ] **Every route shares one title.** Metadata is defined only in
      `layout.tsx`; all four routes present as "OpenSustain Analytics"
- [ ] **Next 15.5.2 advisory.** pnpm flags CVE-2025-66478 on the pinned
      version
- [ ] **`web/tsconfig.tsbuildinfo` is tracked** and churns on every
      typecheck; it belongs in `.gitignore`
- [ ] Update `README.md` — it does not mention the web app at all

---

## Done

- [x] Ecosystem Overview — `/` (tab 1), rebuilt on D3 with search, export and
      a details panel Streamlit has no equivalent for
- [x] Project Rankings — `/projects` (tab 2), all four Streamlit controls
- [x] Organisation Rankings — `/organizations` (tab 3), category filter ranks
      by that category's score as Streamlit does
- [x] Projects over Time — `/projects` (tab 4), category + bubble-size
- [x] Projects by Organisation — `/organizations` (tab 6), drill-down sunburst
- [x] Topics & Keywords — `/topics` (tab 9), both sliders
- [x] Removed every payload from the RSC stream:
      `/` 5,053,147 → 34,164 B · `/projects` 3,185,936 → 22,760 B ·
      `/organizations` 725,193 → 23,014 B · `/topics` 218,335 → 27,934 B
- [x] Mobile navigation — below 768px the nav was `display:none` with nothing
      in its place, so three routes were unreachable on a phone
